import cd from './cd';
import ls from './ls';
import mkdir from './mkdir';
import cat from './cat';
import help from './help';
import pwd from './pwd';
declare const _default: {
    cd: typeof cd;
    ls: typeof ls;
    mkdir: typeof mkdir;
    cat: typeof cat;
    help: typeof help;
    pwd: typeof pwd;
};
export default _default;
