import cd from './cd';
import ls from './ls';
import mkdir from './mkdir';
import cat from './cat';
import help from './help';
import pwd from './pwd';

export default { cd, ls, mkdir, cat, help, pwd };
