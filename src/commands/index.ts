import cd from './cd';
import { ls, lsAutoComplete } from './ls';
import mkdir from './mkdir';
import cat from './cat';
import help from './help';
import pwd from './pwd';
import rm from './rm';

export default { cd, ls, lsAutoComplete, mkdir, cat, help, pwd, rm };
